<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Android 13+ runtime permission for the auto sync local notification (#13). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Required by permission_handler to open the system "ignore battery optimizations" dialog (#3). -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
            android:label="天使动漫"
            android:name="${applicationName}"
            android:icon="@mipmap/ic_launcher">
        <activity
                android:name=".MainActivity"
                android:exported="true"
                android:launchMode="singleTop"
                android:theme="@style/Theme.TsdmClient.Launcher"
                android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
                android:hardwareAccelerated="true"
                android:windowSoftInputMode="adjustResize">
            <!-- 添加这行：禁用 Flutter 引擎的自动 Deep Link 处理，改为由 MainActivity 主动获取 -->
            <meta-data
                    android:name="flutter_deeplinking_enabled"
                    android:value="false" />
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
                    android:name="io.flutter.embedding.android.NormalTheme"
                    android:resource="@style/NormalTheme"
            />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- 【Deep Link 监听】捕获论坛链接，支持在外部点击直接跳转App -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- 对应解析器 isForumHost 中支持的 www.tsdm39.com -->
                <data android:scheme="https" android:host="www.tsdm39.com" android:pathPrefix="/forum.php" />
                <!-- 对应解析器 isForumHost 中支持的 tsdm39.com（不带 www） -->
                <data android:scheme="https" android:host="tsdm39.com" android:pathPrefix="/forum.php" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
                android:name="flutterEmbedding"
                android:value="2"/>
        <!-- GitHub #28: the rotation transition on a HarmonyOS 4.2 phone shows black while the app is drawing
             through Impeller; the Flutter app the reporter compared with (Kazumi) draws through Skia. This is the
             single variable of the comparison build: everything else stays as it is. -->
        <meta-data
                android:name="io.flutter.embedding.android.EnableImpeller"
                android:value="false"/>
        <!-- 后台消息服务。用 specialUse 类型而不是 dataSync：
             Android 15 对 dataSync 前台服务有 24 小时内累计 6 小时的超时限制，
             超时后系统会强制停止服务并可能杀死应用。specialUse 不在超时限制列表里，
             所以能长时间保持运行。 -->
        <service
              android:name="id.flutter.flutter_background_service.BackgroundService"
              android:foregroundServiceType="specialUse"
              android:exported="false"
              tools:replace="android:exported">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keep user logged in to the forum and poll new messages in background" />
        </service>
    </application>
</manifest>
